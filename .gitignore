__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
build.log
*.log
*.part
*.tmp
*.temp
releases/*.mrpack
releases/*.zip
releases/*.sha256
releases/SHA256SUMS
.vscode/
.minecraft/
*.iml
.idea/

# Test pipeline output and shared download cache (scripts/test_pipeline.py
# --output-dir/--cache-dir default to test-results/ and .cache/, but ad hoc
# manual runs sometimes use a suffixed dir, e.g. test-results-server/) --
# always regenerable, never a second source of truth.
/test-results*/
/.cache/
